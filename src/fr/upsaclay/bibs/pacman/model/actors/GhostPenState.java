package fr.upsaclay.bibs.pacman.model.actors;

/**
 * The different state a ghost can be relatively to the ghost pen
 * (not used before step 3 of the project)
 */
public enum GhostPenState {
    OUT, IN, GET_OUT, GET_IN
}
